---
import { cn } from "@/lib/utils"

export interface Props {
  class?: string
  [key: string]: any
}

const { class: className, ...props } = Astro.props

const classes = cn(
  "placeholder:text-muted-foreground focus-visible:border-accent-foreground/40 focus-visible:ring-accent-foreground/20 aria-invalid:ring-destructive/20 dark:aria-invalid:ring-destructive/40 aria-invalid:border-destructive bg-card/20 text-card-foreground flex field-sizing-content min-h-16 w-full rounded-4xl border border-accent-foreground/20 backdrop-blur-md supports-[backdrop-filter]:bg-card/10 shadow-xl shadow-black/10 relative overflow-hidden px-4 py-3 text-base transition-[color,box-shadow] outline-none focus-visible:ring-[3px] disabled:cursor-not-allowed disabled:opacity-50 md:text-sm",
  "before:absolute before:inset-0 before:bg-gradient-to-br before:from-accent-foreground/10 before:via-transparent before:to-transparent before:pointer-events-none before:rounded-4xl",
  "after:absolute after:inset-[1px] after:rounded-4xl after:bg-gradient-to-br after:from-white/15 after:via-transparent after:to-transparent after:pointer-events-none",
  className
)
---

<textarea
  data-slot="textarea"
  class={classes}
  {...props}
></textarea>
