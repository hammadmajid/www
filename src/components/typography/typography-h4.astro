---
import { cn } from "@/lib/utils";

interface Props {
  content?: string;
  class?: string;
  size?: "sm" | "base" | "lg" | "xl";
}

const { content, class: className, size = "base" } = Astro.props;

const sizeClasses = {
  sm: "prose-sm",
  base: "prose",
  lg: "prose-lg",
  xl: "prose-xl",
};
---

<h4 class={cn("scroll-m-20 text-secondary-foreground/80 text-xl font-semibold tracking-tight", sizeClasses[size], className)}>
  {content}
  <slot />
</h4>
